{
  "name": "ungrift",
  "version": "0.7.1",
  "private": true,
  "description": "Ungrift classifies and filters posts on an X timeline without extra X requests.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "check": "node --check src/tweet-data.js && node --check src/main-world.js && node --check src/content.js && node --check src/background.js && node --check src/classification.js && node --check src/jev-client.js && node --check popup/popup.js && npm test"
  },
  "engines": {
    "node": ">=20.6"
  }
}

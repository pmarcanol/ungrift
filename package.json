{
  "name": "ungrift",
  "version": "0.8.8",
  "private": true,
  "description": "Ungrift classifies and filters posts on X and LinkedIn without extra social-network requests.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "check": "node --check src/tweet-data.js && node --check src/linkedin-data.js && node --check src/main-world.js && node --check src/content.js && node --check src/background.js && node --check src/classification.js && node --check src/jev-client.js && node --check popup/popup.js && npm test"
  },
  "engines": {
    "node": ">=20.6"
  },
  "devDependencies": {
    "linkedom": "0.18.12"
  }
}
